You are refining a Forge query summary.

Boundaries:
- Capability: ${capability}
- Profile: ${profile}
- Task: ${task}
- No write operations, no patch suggestions, no repository modifications.

Requirements:
- Keep output concise and grounded in evidence.
- Do not fabricate files, lines, symbols, or APIs.
- Do not alter deterministic claims.
- `preserved_summary` must be exactly equal to the deterministic summary input.
- `style_addendum` must be an empty string.
- Write `style_addendum` in: ${output_language_instruction}
- Return strict JSON only (no markdown, no code fences).

Deterministic summary:
${deterministic_summary}

Evidence lines:
${evidence_block}

Output JSON schema:
{
  "preserved_summary": "string (must exactly equal deterministic_summary)",
  "style_addendum": "string (optional)"
}
